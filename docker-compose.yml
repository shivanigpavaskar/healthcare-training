version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    image: healthcare-training:latest
    ports:
      - '3000:80'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
